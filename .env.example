# FacilityMind 环境配置
# 留空则自动进入"离线规则模式"，使用内置知识库，开箱即跑，无需任何 API Key。

# 填入后切换为真实大模型推理（兼容任意 OpenAI 接口，可指向国内中转/本地模型）
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=gpt-4o-mini

# ---- 示例：接入 DeepSeek（OpenAI 兼容）----
# 1. 到 https://platform.deepseek.com 申请 API Key（sk- 开头）
# 2. 复制本文件为 .env，填入下面三行（去掉前面 # 注释）：
# LLM_API_KEY=sk-你的真实密钥
# LLM_BASE_URL=https://api.deepseek.com
# LLM_MODEL=deepseek-chat        # 或 deepseek-reasoner（思考/推理模式）
#
# 接入后：CLI 与 eval 自动进入"在线 LLM"模式，模型会真正驱动诊断/分类结论，
# 且任意 API 错误都会安全回退到规则库。eval 报告里的 Token 消耗即为真实用量。
